import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Dev-Server auf allen Interfaces (für Handy-Test im Netzwerk)
  // Wird nur im Dev-Modus genutzt
};

export default nextConfig;
